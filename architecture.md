# SwapCycle Architecture

Flutter UI → Firestore → AI preference normalization → weighted directed exchange graph → BGCC matching engine → match result.

AI boundary: AI converts natural-language preferences into structured fields. It does not select partners or cycles. BGCC remains deterministic.
